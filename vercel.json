{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "build", // Изменено с "dist" на "build"
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ],
  "github": {
    "silent": true
  }
}
